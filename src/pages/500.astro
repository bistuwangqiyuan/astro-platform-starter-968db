---
import BaseLayout from '../layouts/BaseLayout.astro';
import Button from '../components/ui/Button.astro';
---

<BaseLayout title="服务器错误">
  <div class="container mx-auto px-4 py-16">
    <div class="max-w-lg mx-auto text-center">
      <div class="mb-8">
        <h1 class="text-9xl font-bold text-red-200">500</h1>
        <h2 class="text-3xl font-bold text-gray-800 -mt-8">服务器错误</h2>
      </div>
      
      <p class="text-gray-600 mb-8">
        抱歉，服务器遇到了一个错误。我们的技术团队已经收到通知，正在努力解决问题。
      </p>
      
      <div class="space-y-4">
        <Button href="/" variant="primary" class="inline-block">
          返回首页
        </Button>
        
        <div class="text-sm text-gray-500">
          <p>您可以尝试：</p>
          <ul class="mt-2 space-y-1">
            <li>• 刷新页面重试</li>
            <li>• 稍后再访问</li>
            <li>• 清除浏览器缓存</li>
          </ul>
        </div>
      </div>
      
      <div class="mt-12 p-4 bg-gray-100 rounded-lg text-left">
        <p class="text-xs text-gray-500 font-mono">
          错误时间：{new Date().toLocaleString('zh-CN')}
        </p>
      </div>
    </div>
  </div>
</BaseLayout>

<style>
  /* 添加一些动画效果 */
  h1 {
    animation: pulse 2s ease-in-out infinite;
  }
  
  @keyframes pulse {
    0%, 100% {
      opacity: 1;
    }
    50% {
      opacity: 0.5;
    }
  }
</style>